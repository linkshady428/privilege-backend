[build]
builder = "dockerfile"

[deploy]
preDeployCommand = "sh -c 'echo PRE_DEPLOY_CALLED && exit 1'"
startCommand = "./server"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "always"
