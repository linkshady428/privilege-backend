[build]
builder = "dockerfile"

[deploy]
releaseCommand = "migrate -path /app/migrations -database $DATABASE_URL up"
startCommand = "./server"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "always"
