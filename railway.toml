[build]
builder = "dockerfile"

[deploy]
preDeployCommand = "sh -c 'migrate -verbose -path /app/migrations -database $DATABASE_URL?sslmode=disable up'"
startCommand = "./server"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "always"
